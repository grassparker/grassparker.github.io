Quick Vite app scaffold

Commands (run inside `app/`):

- Install deps: `npm install`
- Dev server: `npm run dev`
- Build (outputs to repo `dist/`): `npm run build`
- Preview production build: `npm run preview`

Notes:
- Build output is configured to `../dist` so you can integrate it with the site.
